/** Shown on the Create Newsletter form — not collected in Settings when adding a business. */
export const DEFAULT_TOPIC_FIELD_LABEL = "Topic Name";
export const DEFAULT_ORG_FIELD_LABEL = "Organization";

/**
 * Used when generating AI content from Exa; topic-specific context is added at newsletter creation.
 */
export const DEFAULT_BUSINESS_PROMPT_CONTEXT =
  "professional education, training, courses, career development, and industry trends";
